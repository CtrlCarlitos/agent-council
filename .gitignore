/bin/
/dist/
/coverage.out
/.bootstrap/
/.council/
/runs/
/.worktrees/
*.local.json
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.sqlite
*.sqlite-*
*.db
*.db-*
__pycache__/
*.pyc
.DS_Store

# Local tooling state (never repository content)
.serena/
graft/

# AC-008 manual integration evidence (native output, prompts, paths — never repository content)
ac008-evidence/

# AC-009 manual integration evidence (native output, prompts, paths — never repository content)
ac009-evidence/

# AC-010 manual integration evidence (native output, prompts, paths — never repository content)
ac010-evidence/
